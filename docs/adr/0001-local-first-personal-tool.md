# Local-first personal tool

This app serves a single trader and stores all data locally — no server, no accounts, no sync. Multi-user hosting was considered and rejected: it adds auth, security, and hosting surface area that serves nobody, since the only real user is the author. Market data enters manually or via client-side fetch.
